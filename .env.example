# Environment variables for docker-compose.yml
# Copy this file to .env and modify as needed

# Port configuration
SSE_PORT=5560

# Database path (shared between both containers)
DB_PATH=./db

# Configuration file paths
SSE_CONFIG_PATH=./srv/sse-mcp-server/appsettings.json
STDIO_CONFIG_PATH=./srv/stdio-mcp-server/appsettings.json